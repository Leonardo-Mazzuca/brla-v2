


export const isUsdcToUsdc = (input: string, output: string) => {


    return input === 'USDC' && output === 'USDT';

}

export const isUsdtToUsdt = (input: string, output: string) => {


    return input === 'USDT' && output === 'USDT';

}